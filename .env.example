# This app does not auto-load .env files by itself.
# Export these variables in your shell, process manager, or VPS service config.

PORT=4173
DATA_DIR=/absolute/path/to/local-data
AUTH_ENABLED=true
APP_USERNAME=HT-Admin
APP_PASSWORD=change-me
SESSION_COOKIE_NAME=cts_session
SESSION_MAX_AGE_SEC=604800
